import React from 'react';
import { StyledSectionCards } from './style';

const SectionCards = ({ children }) => {
  return <StyledSectionCards>{children}</StyledSectionCards>;
};

export { SectionCards };
